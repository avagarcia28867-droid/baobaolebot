# fly.toml app configuration file generated for <你的APP名字>

app = "baobaolebot"  # ⚠️ 这里改成你在 Fly 网页上创建的 App 名字
primary_region = "sin" # ⚠️ 这里改成你选的地区 (例如 sin, nrt, hkg)

[build]

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = 1
  processes = ["app"]

# 🔥 核心配置：开启 512MB 虚拟内存，防止 256MB 物理内存溢出
[swap_size_mb]
  size = 512